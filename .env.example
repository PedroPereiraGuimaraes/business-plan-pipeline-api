# Project Settings
PROJECT_NAME="Business Plan Pipeline"
API_V1_STR="/api/v1"

# Database (Supabase)
# IMPORTANTE: Use a "Connection String" do "Connection Pooler" (Supavisor) para suportar IPv4.
# Vá em Settings -> Database -> Connection String -> URI -> Habilite "Use connection pooling"
# Mode: Transaction (Recomendado para Serverless/API) - Porta 6543
# Exemplo: postgresql://postgres.projectref:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
DATABASE_URL="postgresql://postgres.projectref:password@aws-0-us-east-1.pooler.supabase.com:6543/postgres"

# Security
# Gere uma chave segura randomica (ex: `openssl rand -hex 32`)
SECRET_KEY="change-this-to-a-secure-random-key"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30

# AI Configuration
GEMINI_API_KEY="sua-chave-api-aqui"

# CORS
# Lista de origens permitidas (frontend). Se for string, deve ser JSON encoded ou separada por virgula se implementado
BACKEND_CORS_ORIGINS=["http://localhost", "http://localhost:3000"]

# Email Configuration (SMTP)
MAIL_USERNAME="youremail@example.com"
MAIL_PASSWORD="yourpassword"
MAIL_FROM="noreply@businessplanpipeline.com"
MAIL_PORT=587
MAIL_SERVER="smtp.example.com"
MAIL_FROM_NAME="Business Plan Pipeline"
MAIL_STARTTLS=True
MAIL_SSL_TLS=False
USE_CREDENTIALS=True
VALIDATE_CERTS=True
